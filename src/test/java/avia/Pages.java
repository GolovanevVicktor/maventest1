/*
 * To change this license header, choose License Headers in Project Properties.
 * To change this template file, choose Tools | Templates
 * and open the template in the editor.
 */
package avia;

import java.util.HashMap;
import java.util.Map;
import org.openqa.selenium.WebDriver;

public class Pages {
   /* Map pages=new HashMap<String,Object>();
    WebDriver web;
    public Pages(WebDriver web){
    this.web=web;}
    public SearchPage createSearchPage(){
    if(pages.get("SearchPage")==null) {
        SearchPage s=new SearchPage(web);
        pages.put("SearchPage",s);
        return s;
        } else {
        return (SearchPage) pages.get("SearchPage");
        }
    }
    public RecommendPage createRecommendPage(){
    if(pages.get("RecommendPage")==null) {
        RecommendPage r=new RecommendPage(web);
        pages.put("RecommendPage",r);
        return r;
        } else {
        return (RecommendPage) pages.get("RecommendPage");
        }
    }
    */
}
